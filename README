REQUIREMENTS
rvm with ruby 1.9.2, Rails 3.0.x

USAGE

Clone the templates folder to the folder that all your rails apps are in or change the path in the command below:

$ rails new appname -m templates/template.rb
$ cd appname
$ gem install bundler
$ bundle install